---
import Logo from './Logo.astro';
import Icon from './Icon.astro';
import PcbTrace from './PcbTrace.astro';
import Title from './Title.astro';
import { agent, markets, legal } from '../data/site';
import { withBase } from '../lib/paths';

const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container">
    <PcbTrace variant="divider" class="site-footer__trace" />

    <div class="site-footer__grid">
      <div class="site-footer__lockup">
        <Logo variant="lockup" />
      </div>

      <nav class="site-footer__col" aria-label="Markets">
        <h2 class="eyebrow">Markets</h2>
        <ul>
          {markets.map((m) => <li><a href={withBase(m.href)}>{m.name}</a></li>)}
        </ul>
      </nav>

      <nav class="site-footer__col" aria-label="Explore">
        <h2 class="eyebrow">Explore</h2>
        <ul>
          <li><a href={withBase('/projects')}>Projects</a></li>
          <li><a href={withBase('/contact')}>Contact</a></li>
          <li>
            <a href={agent.compassProfile} target="_blank" rel="noopener noreferrer" class="site-footer__ext">
              Search Homes on Compass <Icon name="arrow-ext" size={12} /><span class="sr-only">(opens in a new tab)</span>
            </a>
          </li>
        </ul>
        <div class="site-footer__rentals">
          <span class="site-footer__label">Vacation rentals</span>
          <a href={agent.vacationRentals.url} target="_blank" rel="noopener noreferrer" class="site-footer__ext">
            {agent.vacationRentals.name} <Icon name="arrow-ext" size={12} /><span class="sr-only">(opens in a new tab)</span>
          </a>
          <span class="site-footer__note">Independently operated. Compass is not involved in rentals.</span>
        </div>
      </nav>

      <div class="site-footer__col">
        <h2 class="eyebrow">Connect</h2>
        <ul>
          <li><a href={agent.phoneHref}>{agent.phone}</a></li>
          <li><a href={`mailto:${agent.email}`}>{agent.email}</a></li>
          <li>{agent.office.street}<br />{agent.office.city}, {agent.office.state} {agent.office.zip}</li>
        </ul>
        <ul class="site-footer__social" aria-label="Social">
          <li><a href={agent.social.instagram} target="_blank" rel="noopener noreferrer" aria-label="David Weis on Instagram (opens in a new tab)"><Icon name="instagram" size={18} /></a></li>
          <li><a href={agent.social.linkedin} target="_blank" rel="noopener noreferrer" aria-label="David Weis on LinkedIn (opens in a new tab)"><Icon name="linkedin" size={18} /></a></li>
          <li><a href={agent.social.facebook} target="_blank" rel="noopener noreferrer" aria-label="David Weis on Facebook (opens in a new tab)"><Icon name="facebook" size={18} /></a></li>
        </ul>
      </div>
    </div>

    <div class="site-footer__legal">
      <div class="site-footer__legal-col">
        <p class="eyebrow">Compliance</p>
        <div class="site-footer__marks">
          <img src={withBase('/images/equal-housing-opportunity-logo.png')} alt="Equal Housing Opportunity" width="481" height="514" loading="lazy" decoding="async" />
          <img src={withBase('/images/realtor-logo.png')} alt="REALTOR®" width="248" height="286" loading="lazy" decoding="async" />
        </div>
      </div>
      <div class="site-footer__legal-col">
        <p class="eyebrow">Licensing</p>
        <p class="site-footer__lines">
          <span>{agent.name} | <Title /> | CA DRE #{agent.dre}</span>
          <span>{legal.officeLine}</span>
          <span>{legal.brokerLine}</span>
        </p>
      </div>
      <div class="site-footer__legal-col site-footer__legal-col--wide">
        <p class="eyebrow">Disclosure</p>
        <p class="site-footer__disclaimer">{legal.disclaimer}</p>
      </div>
    </div>

    <div class="site-footer__bottom">
      <p class="site-footer__copy">© {year} {agent.name} · <Title /> · CA DRE #{agent.dre}</p>
      <a class="site-footer__top" href="#main">Back to top <Icon name="arrow-right" size={12} /></a>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--surface);
    border-top: 1px solid var(--line);
    padding-block: 2.5rem 2.5rem;
  }
  .site-footer__trace {
    margin-bottom: 3.5rem;
    opacity: 0.9;
  }

  /* Row 1: lockup + link columns */
  .site-footer__grid {
    display: grid;
    grid-template-columns: 1.7fr 0.8fr 1.3fr 1.1fr;
    gap: 3rem 2.5rem;
    align-items: start;
  }
  .site-footer__lockup {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }
  .site-footer__col ul {
    list-style: none;
    display: grid;
    gap: 0.6rem;
    margin-top: 1.1rem;
    font-size: 0.9375rem;
    color: var(--ink-2);
    line-height: 1.6;
  }
  .site-footer__col a {
    text-decoration: none;
    color: var(--ink-2);
  }
  .site-footer__col a:hover {
    color: var(--ink);
    text-decoration: underline;
  }
  .site-footer__ext {
    display: inline;
  }
  .site-footer__ext svg {
    display: inline-block;
    vertical-align: -0.05em;
    margin-left: 0.3em;
  }
  .site-footer__rentals {
    display: grid;
    gap: 0.25rem;
    margin-top: 1.6rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--line);
    font-size: 0.9375rem;
    color: var(--ink-2);
    line-height: 1.6;
  }
  .site-footer__label {
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--muted);
  }
  .site-footer__note {
    font-size: 0.75rem;
    color: var(--muted);
    line-height: 1.5;
  }
  .site-footer__social {
    display: flex !important;
    justify-content: start;
    gap: 0.6rem !important;
    margin-top: 1.25rem !important;
  }
  .site-footer__social a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border: 1px solid var(--line-control);
    border-radius: 999px;
    transition: border-color 0.2s var(--ease), color 0.2s var(--ease);
  }
  .site-footer__social a:hover {
    border-color: var(--ink);
    color: var(--ink);
  }

  /* Row 2: wide legal band */
  .site-footer__legal {
    display: grid;
    grid-template-columns: 0.9fr 1.5fr 1.9fr;
    gap: 2.5rem 3rem;
    margin-top: 3.5rem;
    padding-top: 2.25rem;
    border-top: 1px solid var(--line);
    color: var(--muted);
    font-size: 0.8125rem;
    line-height: 1.65;
  }
  .site-footer__legal-col {
    display: grid;
    gap: 1rem;
    align-content: start;
  }
  .site-footer__marks {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }
  .site-footer__marks img {
    height: 76px;
    width: auto;
  }
  .site-footer__lines {
    display: grid;
    gap: 0.3rem;
    color: var(--ink-2);
    font-size: 0.875rem;
    font-weight: 400;
  }
  .site-footer__disclaimer {
    max-width: 62ch;
  }

  /* Row 3 */
  .site-footer__bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    margin-top: 2.25rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--line);
    color: var(--muted);
    font-size: 0.75rem;
  }
  .site-footer__top {
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-decoration: none;
    color: var(--ink-2);
  }
  .site-footer__top svg {
    transform: rotate(-90deg);
  }
  .site-footer__top:hover {
    color: var(--ink);
  }

  @media (max-width: 1023px) {
    .site-footer__grid {
      grid-template-columns: 1fr 1fr 1fr;
    }
    .site-footer__lockup {
      grid-column: 1 / -1;
    }
    .site-footer__legal {
      grid-template-columns: 1fr 1fr;
    }
    .site-footer__legal-col--wide {
      grid-column: 1 / -1;
    }
  }
  @media (max-width: 640px) {
    .site-footer__grid,
    .site-footer__legal {
      grid-template-columns: 1fr;
      gap: 2.25rem;
    }
    .site-footer__bottom {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
